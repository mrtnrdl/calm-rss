services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - app-data:/data
    restart: unless-stopped
    env_file:
      - .env

volumes:
  app-data:
